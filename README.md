# Emotion-Detection
This is a basic mini project that aims to strengthen concepts in Python such as creating a Package, using it to get a result, make a basic Webapp with Flask that handles the requests between the server and the client and handles related errors as well
